import SwiftUI

// Settings UI is the web app (flynnai.app/app/settings).
// This stub keeps the Settings scene in FlynnDesktopApp.swift compiling.
struct SettingsView: View {
    var body: some View { EmptyView() }
}
