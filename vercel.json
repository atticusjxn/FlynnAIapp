{
  "version": 2,
  "builds": [
    {
      "src": "flynn-ai-new-landingpage/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "index.html" }
  ]
}
