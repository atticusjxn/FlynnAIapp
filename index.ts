import 'react-native-url-polyfill/auto';
import { AppRegistry } from 'react-native';
import App from './App';

// Use basic React Native registration instead of Expo
AppRegistry.registerComponent('main', () => App);
